{
  "name": "tomiarmyclaim",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ saifdevblochain/tomiarmyclaim",
    "create-local": "graph create --node http://localhost:8020/ saifdevblochain/tomiarmyclaim",
    "remove-local": "graph remove --node http://localhost:8020/ saifdevblochain/tomiarmyclaim",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 saifdevblochain/tomiarmyclaim",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.56.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
